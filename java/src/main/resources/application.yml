spring:
  datasource:
    url: jdbc:mysql://0.0.0.0/test?useSSL=false
    username: root
    password: ''
  flyway:
    baseline-on-migrate: true
mybatis:
  type-aliases-package: com.example.demo.model
  configuration:
    map-underscore-to-camel-case: true
logging.level.com.example.demo.repository:
  GroupRepository: DEBUG
  MemberRepository: DEBUG
